name: KFV / ÖFB Tabellen Freitag bis Sonntag

on:
  workflow_dispatch:
  schedule:
    # Freitag, Samstag und Sonntag um 18:00 UTC.
    # Das entspricht während der Sommerzeit 20:00 Uhr in Österreich.
    - cron: "0 18 * * 5,6,0"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sync:
    name: Tabellen synchronisieren
    runs-on: ubuntu-latest
    timeout-minutes: 15

    env:
      TZ: Europe/Vienna
      FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}

    steps:
      - name: Projekt laden
        uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Abhängigkeiten installieren
        run: npm ci

      - name: Firebase-Zugang prüfen
        shell: bash
        run: |
          if [ -z "$FIREBASE_SERVICE_ACCOUNT" ]; then
            echo "::error::Das Repository-Secret FIREBASE_SERVICE_ACCOUNT fehlt."
            exit 1
          fi
          echo "Firebase-Service-Account ist vorhanden."

      - name: Projekt und Tabellen-Synchronisierung prüfen
        run: npm run sync:check

      - name: Tabellen synchronisieren
        run: npm run sync:tables

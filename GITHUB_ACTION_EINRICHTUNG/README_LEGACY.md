# Historische Einrichtungsdateien

Die produktive Workflow-Datei liegt ab Version 11.0.2a ausschließlich hier:

```text
.github/workflows/kfv-sync.yml
```

Die Dateien in diesem Ordner dienen nur als ältere Schritt-für-Schritt-Hilfe. Änderungen am produktiven Workflow bitte nur unter `.github/workflows/` durchführen.

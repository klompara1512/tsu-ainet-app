name: ÖFB Kampfmannschaft Tabelle exakt

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: oefb-km-table-${{ github.ref }}
  cancel-in-progress: true

jobs:
  exact-table:
    name: Kampfmannschaft Tabelle synchronisieren
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      TZ: Europe/Vienna
      TABLE_TEAM: KM
      FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
    steps:
      - name: Projekt laden
        uses: actions/checkout@v4
      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Abhängigkeiten installieren
        run: npm ci
      - name: Firebase-Zugang prüfen
        shell: bash
        run: |
          if [ -z "$FIREBASE_SERVICE_ACCOUNT" ]; then
            echo "::error::Repository-Secret FIREBASE_SERVICE_ACCOUNT fehlt."
            exit 1
          fi
      - name: Spieltagsprüfung
        id: matchday
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
        run: node scripts/table-matchday-gate.cjs KM

      - name: Kampfmannschaft Parser Selbsttest
        if: steps.matchday.outputs.should_run == 'true'
        run: node scripts/exact-team-table-selftest.cjs KM
      - name: Exakte Kampfmannschaft Tabelle synchronisieren
        if: steps.matchday.outputs.should_run == 'true'
        run: node scripts/exact-team-table-sync.cjs KM

# Version 8.4.3 – Logos im Dashboard

- `TeamLogo` lädt Vereinslogos jetzt zentral aus `kfvClubs`.
- Bestehende Logo-URLs aus `kfvMatches` bleiben als Fallback erhalten.
- Das TSU-Ainet-Logo wird lokal geladen.
- Dölsach kann optional lokal unter `public/logos/clubs/doelsach.png` abgelegt werden.
- Der Fix gilt automatisch für Dashboard, Spielcenter und weitere Bereiche, die `TeamLogo` verwenden.
